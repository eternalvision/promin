import Main from "./Main";
import Profile from "./Profile";
import { LayoutPages } from "./LayoutPages";

import { ResableComponents } from "../blocks/ResableComponents";

export const RouteComponents = {
  ResableComponents,
  Main,
  Profile,
  LayoutPages,
};
